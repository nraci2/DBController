import time
import threading


class Drawbridge:

    def __init__(self, ismoving, isup, isdown):

        self.ismoving = "In Transit: " + str(ismoving)
        self.isup = isup
        self.isdown = isdown




